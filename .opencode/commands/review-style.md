---
description: Review code against Haishin Swift style guidelines
agent: code-reviewer
subtask: true
---

Review the code in $ARGUMENTS against the Haishin iOS conventions.

If no file is specified, review the most recently edited Swift file.

The `code-reviewer` agent will load the relevant skills (`swift-style`, `new-feature`) and report violations with file:line references and skill citations.
